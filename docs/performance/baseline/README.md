# Performance baseline artifacts

The baseline artifacts live in the parent directory so existing paths and
provenance remain stable.

- `../startup-baseline.json`
- `../semantic-after.json` is a semantic follow-up artifact, not a baseline.
- `../operations-baseline.json`
- `../operations-baseline-1000.json`

The baseline revision is `b2129e7`, the post-hygiene main tip used by the
performance audit. The immutable release reference is `v0.6.0` at
`033c1484dca53fbb40dbf82904aacf5f2834142a`. These files record measurements,
not capacity guarantees. M009 evidence under `../../m009/` remains unchanged.
